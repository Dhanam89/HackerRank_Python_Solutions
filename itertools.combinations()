from itertools import combinations

# Read input string and integer
s, k = input().split()
k = int(k)

# Sort the string to ensure lexicographic order
s = sorted(s)

# Generate combinations of all sizes from 1 to k and print them
for r in range(1, k + 1):
    for combo in combinations(s, r):
        print(''.join(combo))
