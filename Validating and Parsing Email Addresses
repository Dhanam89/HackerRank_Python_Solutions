import re
import email.utils

pattern = r'^[a-zA-Z][\w\.\-]*@[a-zA-Z]+\.[a-zA-Z]{1,3}$'

n = int(input())
for _ in range(n):
    line = input().strip()
    name, addr = email.utils.parseaddr(line)
    if re.match(pattern, addr):
        print(line)
